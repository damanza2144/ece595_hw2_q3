<%= div_for comment do %>
	<% @user = User.find(comment.user_id) %>
	<p><strong>
		Posted <%= time_ago_in_words(comment.created_at) %> by <%= @user.name %>
	</strong><br />
	<%= h(comment.body) %>
	</p>
<% end %>
